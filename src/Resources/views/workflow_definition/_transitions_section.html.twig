{% set collection = form.transitions %}

<div
    class="workflow-collection"
    data-collection-key="transitions"
    data-form-prefix="{{ collection.vars.full_name }}"
    data-prototype="{{ form_widget(collection.vars.prototype)|e('html_attr') }}"
    data-index="{{ collection|length }}"
    data-modal-id="transition-editor-modal"
    data-place-names="{{ definition.placeNames|json_encode|e('html_attr') }}"
    data-place-placeholder="{{ 'form.placeholder.select_places'|trans({}, 'nowo_workflow')|e('html_attr') }}"
>
    <div class="d-flex flex-wrap justify-content-between align-items-center gap-2 mb-3">
        <p class="text-muted small mb-0">{{ 'form.help.transition_places'|trans({}, 'nowo_workflow') }}</p>
        <button type="button" class="btn btn-sm btn-primary" data-collection-add>
            {{ 'action.add_transition'|trans({}, 'nowo_workflow') }}
        </button>
    </div>

    <div class="table-responsive border rounded bg-white">
        <table class="table table-hover align-middle mb-0">
            <thead class="table-light">
                <tr>
                    <th>{{ 'table.name'|trans({}, 'nowo_workflow') }}</th>
                    <th>{{ 'form.field.label'|trans({}, 'nowo_workflow') }}</th>
                    <th>{{ 'form.field.from_places'|trans({}, 'nowo_workflow') }}</th>
                    <th>{{ 'form.field.to_places'|trans({}, 'nowo_workflow') }}</th>
                    <th class="text-end">{{ 'table.actions'|trans({}, 'nowo_workflow') }}</th>
                </tr>
            </thead>
            <tbody data-collection-table></tbody>
        </table>
        <p class="text-muted text-center py-4 mb-0 d-none" data-collection-empty>{{ 'collection.no_transitions'|trans({}, 'nowo_workflow') }}</p>
    </div>

    <div class="d-none" data-collection-entries>
        {% for transitionForm in collection %}
            <div data-collection-entry="{{ loop.index0 }}">{{ form_widget(transitionForm) }}</div>
        {% endfor %}
    </div>
</div>

<div class="modal fade" id="transition-editor-modal" tabindex="-1" aria-labelledby="transition-editor-modal-label" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered modal-lg">
        <div class="modal-content">
            <div class="modal-header">
                <h2 class="modal-title h5" id="transition-editor-modal-label" data-modal-title-add="{{ 'modal.add_transition'|trans({}, 'nowo_workflow') }}" data-modal-title-edit="{{ 'modal.edit_transition'|trans({}, 'nowo_workflow') }}"></h2>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="{{ 'action.close'|trans({}, 'nowo_workflow') }}"></button>
            </div>
            <div class="modal-body" data-modal-body></div>
            <div class="modal-footer">
                <button type="button" class="btn btn-outline-secondary" data-modal-cancel>{{ 'action.cancel'|trans({}, 'nowo_workflow') }}</button>
                <button type="button" class="btn btn-primary" data-modal-apply>{{ 'action.apply'|trans({}, 'nowo_workflow') }}</button>
            </div>
        </div>
    </div>
</div>
