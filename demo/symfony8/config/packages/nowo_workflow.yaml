nowo_workflow:
    enabled: true
    ui:
        path: '/workflow'
        default_locale: 'en'
        locales: ['en', 'es', 'fr', 'it']
    security:
        # Demo has no SecurityBundle — local only (REQ-UI-002).
        allow_unauthenticated: true
