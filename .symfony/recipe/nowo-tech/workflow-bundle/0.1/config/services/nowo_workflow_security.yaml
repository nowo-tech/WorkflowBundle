# Optional: override the auto-wired RoleBasedWorkflowUiAccessChecker
# (WorkflowUiSecurityPass wires it when SecurityBundle is present).
#
# services:
#     Nowo\WorkflowBundle\Contract\WorkflowUiAccessCheckerInterface:
#         alias: App\Security\WorkflowUiAccessChecker
#
# Or set nowo_workflow.security.access_checker: App\Security\WorkflowUiAccessChecker
# Prefer also protecting ui.path with security.access_control.
